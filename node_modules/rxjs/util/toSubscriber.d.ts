import { PartialObserver } from '../Observer';
import { Subscriber } from '../Subscriber';
export declare function toSubscriber<R>(nextOrObserver?: PartialObserver<T> | ((value: T) => void), error?: (error: any) => void, complete?: () => void): Subscriber<R>;
